'use strict'
import Mlux from 'mlux';

var manager = Mlux.manager;
var storeConfigs = [
    {
        name:'user',
        model:{
            username:'未知',
            age:0,
            gender:'未知'
        }
    }
];

export default storeConfigs;